# Wallet Tree

Read `AGENT.md` and the scoped rules in `.claude/rules/` before changing code. Keep the NestJS/Vite/MongoDB/shared package boundaries intact. Regular users authenticate only with LINE; local username/password authentication is admin-only.
