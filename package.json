{
  "name": "wallet-tree",
  "private": true,
  "version": "1.0.0",
  "description": "Personal income and expense tracking application",
  "scripts": {
    "dev": "pnpm --filter app dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
