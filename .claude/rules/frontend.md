# Frontend rules

- Use React function components, React Router and Tailwind utilities.
- Call backend endpoints through `src/lib/api.ts`.
- Regular-user login UI exposes LINE Login only.
- Keep admin routes and guards separate from the regular app shell.
- Use Thai UI copy for user-facing finance screens and mobile-first layouts.
